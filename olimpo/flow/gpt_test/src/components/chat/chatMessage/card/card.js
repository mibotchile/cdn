import { WebComponent } from "../../../webComponent";

export class Card extends WebComponent {
  constructor({
    type,
    id,
    name,
    address,
    image_url,
    country,
    location,
    data,
  }) {}
}
